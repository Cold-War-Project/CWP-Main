character_role_ruler = {
	texture = "gfx/interface/icons/character_role_icons/ruler.dds"

	type = ruler
	priority = 100
	
	title_format = ruler
	title_preposition = PREP_OF
	
	character_modifier = {
		character_prominence_add = 100
		character_loyalty_add = 25
	}
	
	auto_assigned = yes
}

character_role_heir = {
	texture = "gfx/interface/icons/character_role_icons/heir.dds"

	type = heir
	priority = 99
	
	title_format = heir
	title_preposition = PREP_OF
	
	character_modifier = {
		character_prominence_add = 50
		character_loyalty_add = 25
	}
	
	auto_assigned = yes
}

character_role_general = {
	texture = "gfx/interface/icons/character_role_icons/general.dds"

	type = general
	priority = 50

	title_format = custom
	title_preposition = PREP_IN

	auto_assigned = yes
	career_length = { 15 30 }
}

character_role_admiral = {
	texture = "gfx/interface/icons/character_role_icons/admiral.dds"

	type = admiral
	priority = 50

	title_format = custom
	title_preposition = PREP_IN

	auto_assigned = yes
	career_length = { 15 30 }
}

character_role_executive = {
	texture = "gfx/interface/icons/character_role_icons/executive.dds"

	type = executive
	priority = 10

	auto_assigned = yes
	career_length = { 10 20 }
}

character_role_magnate = {
	texture = "gfx/interface/icons/character_role_icons/magnate.dds"

	type = magnate
	priority = 10

	auto_assigned = yes
	
	title_format = custom
	title_preposition = PREP_IN
	
	spawn_characters_to_pool = yes
	
	pool_spawn_target_country_wide = {
		value = country_rank # (0 = decentralized, 7 = great power)
		
		# Subtracting by 1 and dividing by 2 results in:
		# Great Power = 3 magnates
		# Major Power/Unrecognized Major Power = 2 magnates
		# Minor Power/Unrecognized Regional Power = 1 magnate
		# Others = no magnates
		subtract = 1
		divide = 2 
	}
	
	# root = country
	holding_scores = {
		holding_manor_houses_score = {
			type = "building_manor_house"
			score = {
				value = 100
				if = {
					limit = {
						scope:is_monarch = yes
					}
					add = 10000
				}
				else_if = {
					limit = {
						scope:interest_group ?= { 
							OR = {
								is_interest_group_type = ig_rural_folk
								is_interest_group_type = ig_landowners	
							}
						}
					}
					add = 1000
				}
				else_if = {
					limit = {
						scope:interest_group ?= { 
							OR = {
								is_interest_group_type = ig_devout
								is_interest_group_type = ig_armed_forces
							}
						}
					}
					add = 200
				}
				
				if = {
					limit = {
						scope:state ?= { NOT = { has_building = building_manor_house } }
					}
					multiply = 0
				}						
			}
		}
		holding_financial_districts_score = {
			type = "building_financial_district"
			score = {
				value = 100				
				if = {
					limit = {
						scope:interest_group ?= { is_interest_group_type = ig_industrialists }
					}
					add = 1000
				}
				else_if = {
					limit = {
						scope:interest_group ?= { 
							OR = {
								is_interest_group_type = ig_petty_bourgeoisie
								is_interest_group_type = ig_trade_unions
							}
						}
					}
					add = 200
				}	

				if = {
					limit = {
						scope:state ?= { NOT = { has_building = building_financial_district } }
					}
					multiply = 0
				}				
			}
		}			
	}
}

character_role_agitator = {
	texture = "gfx/interface/icons/character_role_icons/agitator.dds"

	type = agitator
	priority = 2

	auto_assigned = yes
	career_length = { 10 20 }
	
	character_modifier = {
		character_prominence_add = -10
	}
}

character_role_politician = {
	texture = "gfx/interface/icons/character_role_icons/politician.dds"

	type = politician
	priority = 1

	auto_assigned = yes
	career_length = { 5 10 }
	
	spawn_characters_to_pool = yes
	
	pool_spawn_target_powerful = 4
	pool_spawn_target_influential = 3
	pool_spawn_target_marginal = 0
	
	character_modifier = {
		character_prominence_add = 10
	}
}
