onaction_currency_calculation = {
	# Currency Calculations
	if = {
		limit = {
			var:cwp_currency = flag:currency_usd
			is_target_in_global_variable_list = {
				name = central_bank_list
				target = THIS
			}
			global_var:currency_usd_v >= 0.01
		}
		change_global_variable = {
			name = currency_usd_v
			add = currency_value_growth
		}
	}
	if = {
		limit = {
			var:cwp_currency = flag:currency_frf
			global_var:currency_frf_v >= 0.01
			is_target_in_global_variable_list = {
				name = central_bank_list
				target = THIS
			}
		}
		change_global_variable = {
			name = currency_frf_v
			add = currency_value_growth
		}
	}
	if = {
		limit = {
			var:cwp_currency = flag:currency_gbp
			global_var:currency_gbp_v >= 0.01
			is_target_in_global_variable_list = {
				name = central_bank_list
				target = THIS
			}
		}
		change_global_variable = {
			name = currency_gbp_v
			add = currency_value_growth
		}
	}
	if = {
		limit = {
			var:cwp_currency = flag:currency_sur
			global_var:currency_sur_v >= 0.01
			is_target_in_global_variable_list = {
				name = central_bank_list
				target = THIS
			}
		}
		change_global_variable = {
			name = currency_sur_v
			add = currency_value_growth
		}
	}
	if = {
		limit = {
			var:cwp_currency = flag:currency_brz
			global_var:currency_brz_v >= 0.01
			is_target_in_global_variable_list = {
				name = central_bank_list
				target = THIS
			}
		}
		change_global_variable = {
			name = currency_brz_v
			add = currency_value_growth
		}
	}
	if = {
		limit = {
			var:cwp_currency = flag:currency_dem
			global_var:currency_dem_v >= 0.01
			is_target_in_global_variable_list = {
				name = central_bank_list
				target = THIS
			}
		}
		change_global_variable = {
			name = currency_dem_v
			add = currency_value_growth
		}
	}
	if = {
		limit = {
			var:cwp_currency = flag:currency_pen
			global_var:currency_pen_v >= 0.01
			is_target_in_global_variable_list = {
				name = central_bank_list
				target = THIS
			}
		}
		change_global_variable = {
			name = currency_pen_v
			add = currency_value_growth
		}
	}
	if = {
		limit = {
			var:cwp_currency = flag:currency_pes
			global_var:currency_pes_v >= 0.01
			is_target_in_global_variable_list = {
				name = central_bank_list
				target = THIS
			}
		}
		change_global_variable = {
			name = currency_pes_v
			add = currency_value_growth
		}
	}
	if = {
		limit = {
			var:cwp_currency = flag:currency_dnr
			global_var:currency_dnr_v >= 0.01
			is_target_in_global_variable_list = {
				name = central_bank_list
				target = THIS
			}
		}
		change_global_variable = {
			name = currency_dnr_v
			add = currency_value_growth
		}
	}
	if = {
		limit = {
			var:cwp_currency = flag:currency_irr
			global_var:currency_irr_v >= 0.01
			is_target_in_global_variable_list = {
				name = central_bank_list
				target = THIS
			}
		}
		change_global_variable = {
			name = currency_irr_v
			add = currency_value_growth
		}
	}
	if = {
		limit = {
			var:cwp_currency = flag:currency_trl
			global_var:currency_trl_v >= 0.01
			is_target_in_global_variable_list = {
				name = central_bank_list
				target = THIS
			}
		}
		change_global_variable = {
			name = currency_trl_v
			add = currency_value_growth
		}
	}
	if = {
		limit = {
			var:cwp_currency = flag:currency_yum
			global_var:currency_yum_v >= 0.01
			is_target_in_global_variable_list = {
				name = central_bank_list
				target = THIS
			}
		}
		change_global_variable = {
			name = currency_yum_v
			add = currency_value_growth
		}
	}
	if = {
		limit = {
			var:cwp_currency = flag:currency_pln
			global_var:currency_pln_v >= 0.01
			is_target_in_global_variable_list = {
				name = central_bank_list
				target = THIS
			}
		}
		change_global_variable = {
			name = currency_pln_v
			add = currency_value_growth
		}
	}
}
